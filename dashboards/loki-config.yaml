auth_enabled: false

ingester:
  chunk_idle_period: 3m
  chunk_retain_period: 1m
  max_chunk_age: 1h
  chunk_encoding: snappy
  max_concurrent_flushes: 10
  lifecycler:
    ring:
      kvstore:
        store: inmemory
      replication_factor: 1

limits_config:
  enforce_metric_name: false
  reject_old_samples: true
  reject_old_samples_max_age: 168h
  ingestion_rate_mb: 16
  ingestion_burst_size_mb: 20
  max_streams_per_user: 10000
  max_global_streams_per_user: 10000
  retention_period: 720h  # 30 days retention

schema_config:
  configs:
    - from: 2020-10-24
      store: boltdb-shipper
      object_store: filesystem
      schema: v11
      index:
        prefix: index_
        period: 24h

server:
  http_listen_port: 3100
  log_level: info
  log_format: json

storage_config:
  boltdb_shipper:
    active_index_directory: /loki/boltdb-shipper-active
    cache_location: /loki/boltdb-shipper-cache
    shared_store: filesystem
  filesystem:
    directory: /loki/chunks

chunk_store_config:
  max_look_back_period: 0s

table_manager:
  retention_deletes_enabled: false
  retention_period: 0s

# Query processing
querier:
  max_concurrent: 10
  max_samples: 1000000

query_range:
  align_queries_with_step: true
  cache_results: true
  max_cache_freshness_per_user: 10m
  results_cache:
    cache:
      enable_fifocache: true
      default_validity: 10m
      background:
        writeback_goroutines: 10
        writeback_channel_size: 10000

# Loki-specific settings for security monitoring
runtime_config:
  period: 30s
  file: /etc/loki/rules/runtime.yaml

# Metric extraction from logs
metric_relabel_configs:
  # Extract C2 Detection metrics from LSTM alerts
  - source_labels: [__name__]
    regex: 'container_name;lstm_c2_detector'
    target_label: __tmp_extracted
  # Extract authentication failures
  - source_labels: [__name__]
    regex: 'audit_log;authentication_failure'
    target_label: __tmp_extracted

